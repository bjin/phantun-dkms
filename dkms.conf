PACKAGE_NAME="phantun"
PACKAGE_VERSION="0.1.1"
BUILT_MODULE_NAME[0]="phantun"
BUILT_MODULE_LOCATION[0]="."
DEST_MODULE_LOCATION[0]="/updates"
MAKE[0]="./configure --with-kernel=${kernel_source_dir} && make KDIR=${kernel_source_dir}"
AUTOINSTALL="yes"
